# Google Sheets
GOOGLE_SHEETS_SPREADSHEET_ID=your-spreadsheet-id-here

# The service account JSON key file should be placed at ./credentials.json
# (mounted read-only into the container by docker-compose)
